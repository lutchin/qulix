{% extends '@QulixRecycling//layout.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">

            {{ form_start(form, {'attr': {'id': 'form_template_settings'}}) }}
            <h2>Кто вы?:</h2> <p>{{ form_widget(form.population,{'attr' : {'align' : 'left'}}) }}</p>
            <h2>Что принесли?:</h2> <p>{{ form_widget(form.name) }}</p>


            {{ form_end(form) }}
        </div>
        <div class="col-md-3">
            <h2>{{ '{{ answer.msg  }}' }}</h2>

        </div>
    </div>
{% endblock %}